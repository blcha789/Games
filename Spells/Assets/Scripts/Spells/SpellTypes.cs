using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Spell { Positive, Negative }
public enum SpellType { None, Fire, Water, Electic, Ground, Ice }
public enum SpellEffect { None, Stun, Root, Silence, Slow, Boost }

public class SpellTypes : MonoBehaviour
{
}
